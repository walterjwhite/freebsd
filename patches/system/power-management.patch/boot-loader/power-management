hint.p4tcc.0.disabled=1
hint.acpi_throttle.0.disabled=1

# disable local APIC timers
hint.apic.0.clock=0

# increase inactivity periods (increased latency)
kern.hz=100

hint.atrtc.0.clock=0

hw.pci.do_power_nodriver=3
