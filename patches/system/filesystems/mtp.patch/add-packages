# this does not work with RCA (and it does not even see my ZTE phone)
#sys-fs/mtpfs
# this works on the RCA tablet, but after each operation, it requires an fusermount -u followed by another mount command
#jmtpfs

# worked with LG
fusefs-jmtpfs

#sys-fs/simple-mtpfs

# this is for newer android phones
# adbfs (the github project also didn't work at all)
# this did not work for any of my devices
#dev-util/android-tools

# the only thing that worked actually was gphotofs
# this does not work with RCA
#gphotofs
